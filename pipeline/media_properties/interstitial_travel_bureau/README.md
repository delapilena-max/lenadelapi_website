# The Interstitial Travel Bureau Creative OS V1

This property package makes validated, versioned JSON the creative production authority for The Interstitial Travel Bureau. It stops at deterministic provider-neutral plans and execution-disabled compiled requests. It has no provider, network, scheduler, publishing, secret, or Lena dependency.

Start with `documentation/ITB_JSON_CREATIVE_OS_V1.md`, inspect `documentation/dependency_map_v1.json`, and validate Pilot 001 with:

```text
C:\Python314\python.exe -B -m tools.itb_validate_episode_v1 --episode-root pipeline/media_properties/interstitial_travel_bureau/pilots/pilot_001 --validate-only
```

Pilot 001 is `What to Do When Your Reflection Arrives First`. Its complete fourteen-artifact dataset is under `pilots/pilot_001/`.

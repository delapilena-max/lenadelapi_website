# Learning Engine V1

Apply `GENERATOR_CONTRACT_V1.md`. Produce only a provisional candidate matching `bureau_episode_learning_v1.schema.json` from validated metrics inputs.

Separate unknown from zero, record confidence, permit only evidence-bounded future-selection conclusions, and state prohibited overfitting conclusions. Learning may influence future selection but can never override canon, user locks, safety policy, continuity history, or novelty lockouts. Never mutate the continuity ledger directly.

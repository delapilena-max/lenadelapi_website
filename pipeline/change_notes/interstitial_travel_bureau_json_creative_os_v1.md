# Interstitial Travel Bureau JSON Creative OS V1 Change Note

**Date:** 2026-08-02
**Scope:** New property namespace only; no Lena deployment note or production surface changed.

## What changed

Added the first structured-JSON creative operating system for The Interstitial Travel Bureau: fourteen strict schemas, canonical JSON/SHA authority, governed loading and path containment, cross-file and domain validation, explainable novelty comparison, four deterministic compiler stages, nine generator instruction authorities, structured read-only CLIs, a complete validated Pilot 001 dataset, adversarial fixtures, tests, and architecture documentation.

## Decisions

Validated JSON is production authority; compiled prompts are disposable outputs. Ordinary shorts use 7-10 shots. Pilot 001 uses the narrowly governed `approved_extended_short` profile because its user-authorized direction locks eleven shots. All exact numeric authority uses integers. No third-party schema dependency was added because the Python 3.14 runtime lacks one and V1 uses a small tested subset of schema keywords.

## Safety and scope

No provider, network, media generation, voice, music, scheduler, publishing, secret, `.env`, or monetization action is connected. Compiled requests are provider-unassigned and `execution_authorized=false`. Lena modules, deployment notes, live checkout, scheduler, queues, runtime evidence, identity, HPE, publishers, and video policy were not modified or imported.

## Validation

Pilot 001 validates through the same generic fourteen-artifact path intended for future episodes. Final local results using `C:\Python314\python.exe`:

- ITB focused ring: `57 passed`.
- File-aware compilation: 21 new Python files passed.
- Import smoke: 12 package and CLI modules passed.
- All four structured help paths and all four validate-only CLI paths passed.
- Repeated compilation was byte-equivalent and matched the checked-in outputs.
- Generation-plan SHA-256: `67e49dd783e1fd09e84630fb5f3a6eda661d7f31fdae305efa6a6678d5b9f8bd`.
- Compiled-request SHA-256: `0f76ecf288a69a6e1d70edbe695568ceafd1087e3ef947c58fce5e1aaa6e8282`.
- Deterministic compilation fingerprint: `304d6ee9ace10bfdb7534dc61c7d9314950c86c78c0e4d5b5752e878d8a7670e`.
- Canonical hash stability, stale-output rejection, traversal, symlink/junction escape, and `git diff --check` passed.
- No shared neutral utility changed, so no unrelated Lena live test was run.

## Next authorized step

Review and merge only after CI is green and Nicolas explicitly authorizes merge. Provider integration, media generation, and any Lena reuse remain separate future work.
